import * as React from "react";
import { Switch } from "@base-ui/react/switch";
import { type VariantProps } from "class-variance-authority";
declare const toggleVariants: (props?: ({
    variant?: "default" | "error" | null | undefined;
    size?: "xs" | "sm" | "default" | null | undefined;
} & import("class-variance-authority/types").ClassProp) | undefined) => string;
export interface ToggleProps extends Omit<React.ComponentPropsWithoutRef<typeof Switch.Root>, "checked" | "defaultChecked" | "onCheckedChange">, VariantProps<typeof toggleVariants> {
    /**
     * The controlled checked state of the toggle.
     */
    checked?: boolean;
    /**
     * The default checked state when uncontrolled.
     */
    defaultChecked?: boolean;
    /**
     * Callback fired when the checked state changes.
     */
    onCheckedChange?: (checked: boolean) => void;
    /**
     * Whether the toggle is disabled.
     */
    disabled?: boolean;
    /**
     * The visual variant of the toggle.
     * @default "default"
     */
    variant?: "default" | "error";
    /**
     * The size of the toggle.
     * @default "default"
     */
    size?: "xs" | "sm" | "default";
    /**
     * Optional aria-label for accessibility.
     * Required if no visible label is provided.
     */
    "aria-label"?: string;
}
/**
 * iOS-style toggle switch component
 * Built on Base UI Switch for full accessibility
 *
 * @example
 * ```tsx
 * // Uncontrolled
 * <Toggle defaultChecked aria-label="Enable notifications" />
 *
 * // Controlled
 * <Toggle
 *   checked={enabled}
 *   onCheckedChange={setEnabled}
 *   aria-label="Enable notifications"
 * />
 *
 * // Error state
 * <Toggle variant="error" aria-label="Invalid setting" />
 * ```
 */
declare const Toggle: React.ForwardRefExoticComponent<ToggleProps & React.RefAttributes<HTMLElement>>;
export { Toggle, toggleVariants };
//# sourceMappingURL=toggle.d.ts.map