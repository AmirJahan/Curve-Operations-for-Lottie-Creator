import * as React from "react";
import { type VariantProps } from "class-variance-authority";
import { Button as BaseUIButton } from "@base-ui/react/button";
declare const buttonVariants: (props?: ({
    variant?: "secondary" | "destructive" | "default" | "link" | "outline" | "ghost" | "link-muted" | null | undefined;
    size?: "sm" | "default" | "icon" | null | undefined;
} & import("class-variance-authority/types").ClassProp) | undefined) => string;
export interface ButtonProps extends Omit<React.ComponentPropsWithoutRef<typeof BaseUIButton>, 'render'>, VariantProps<typeof buttonVariants> {
    /**
     * The visual style of the button.
     * @default "default"
     */
    variant?: "default" | "destructive" | "outline" | "secondary" | "ghost" | "link" | "link-muted";
    /**
     * The size of the button.
     * @default "default"
     */
    size?: "sm" | "default" | "icon";
    /**
     * Render the button as a different element (e.g. an anchor tag)
     * @example <Button render={<a href="/" />}>Link</Button>
     */
    render?: React.ReactElement;
    /**
     * If true, the button will show a spinner and be disabled
     */
    loading?: boolean;
}
/**
 * Button component with multiple variants and sizes
 * Optimized for plugin UIs with compact sizing
 *
 * @example
 * ```tsx
 * <Button variant="default">
 *   Click me
 * </Button>
 *
 * <Button variant="destructive" size="sm">
 *   Delete
 * </Button>
 *
 * <Button render={<a href="/link" />}>Link Button</Button>
 * ```
 */
declare const Button: React.ForwardRefExoticComponent<ButtonProps & React.RefAttributes<HTMLButtonElement>>;
export { Button, buttonVariants };
//# sourceMappingURL=button.d.ts.map