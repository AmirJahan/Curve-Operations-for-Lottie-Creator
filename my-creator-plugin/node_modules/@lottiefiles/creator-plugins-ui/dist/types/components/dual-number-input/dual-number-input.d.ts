import * as React from "react";
import { type VariantProps } from "class-variance-authority";
declare const dualNumberInputVariants: (props?: ({
    size?: "sm" | "default" | "lg" | null | undefined;
} & import("class-variance-authority/types").ClassProp) | undefined) => string;
export interface DualNumberInputProps extends Omit<React.HTMLAttributes<HTMLDivElement>, "onChange">, VariantProps<typeof dualNumberInputVariants> {
    /**
     * First value (e.g., width)
     */
    value1: number;
    /**
     * Second value (e.g., height)
     */
    value2: number;
    /**
     * Callback when first value changes
     */
    onChange1: (value: number) => void;
    /**
     * Callback when second value changes
     */
    onChange2: (value: number) => void;
    /**
     * Label for first input (e.g., "W")
     */
    label1?: string;
    /**
     * Label for second input (e.g., "H")
     */
    label2?: string;
    /**
     * Minimum value for both inputs
     */
    min?: number;
    /**
     * Maximum value for both inputs
     */
    max?: number;
    /**
     * Step increment for both inputs
     */
    step?: number;
    /**
     * Unit suffix to display after the number (e.g., "px", "s", "%")
     */
    suffix?: string;
    /**
     * Number of decimal places to show
     */
    decimals?: number;
    /**
     * Sensitivity multiplier for drag changes (default: 0.5)
     */
    dragSensitivity?: number;
    /**
     * Disabled state
     */
    disabled?: boolean;
}
/**
 * DualNumberInput - A combined input for two related numeric values
 *
 * Perfect for width/height, X/Y coordinates, or any paired numeric values.
 * Each input supports drag-to-change and direct editing.
 *
 * @example
 * ```tsx
 * <DualNumberInput
 *   value1={width}
 *   value2={height}
 *   onChange1={setWidth}
 *   onChange2={setHeight}
 *   label1="W"
 *   label2="H"
 * />
 * ```
 */
export declare const DualNumberInput: React.ForwardRefExoticComponent<DualNumberInputProps & React.RefAttributes<HTMLDivElement>>;
export {};
//# sourceMappingURL=dual-number-input.d.ts.map